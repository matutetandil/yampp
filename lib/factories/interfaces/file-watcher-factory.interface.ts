/**
 * Factory interface for creating FileWatcher instances
 */
export interface IFileWatcherFactory {
  createFileWatcher(): any; // Using 'any' temporarily until IFileWatcher interface is properly defined
}